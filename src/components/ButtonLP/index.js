import React from "react";
import Button from 'react-bootstrap/Button';

export default function ButtonLP() {
    return (
        <div>
            <Button variant='secondary' style={{width: '100%'}}>Lanjutkan Pembayaran</Button>
            {/* <Button variant='success' style={{width: '100%'}}>Lanjutkan Pembayaran</Button> */}
        </div>
    )
}